#include <iostream>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;
    for (int j = a; j <= b j++) {
        for (int i = 2; i <= sqrt(a); i++) {
            if (a + 1 % i == 0) cout << "No";
            else cout << "i";
        }
    }
    return 0;
}
