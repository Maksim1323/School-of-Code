// A. Средство от бессонницы
#include<iostream>
using namespace std;

int main()
{
    int k, l, m, n, d, j = 0;
    cin >> k >> l >> m >> n >> d;
    for (int i = 1; i <= d; i++){
        if (i % k == 0) ++j;
        else if (i % l == 0) ++j;
        else if (i % m == 0) ++j;
        else if (i % n == 0) ++j;
    }
    cout << j;
    return 0;
}
