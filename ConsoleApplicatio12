#include <iostream>
using namespace std;
     
int main()
{
    int n, s, c = 0;
    cin >> n;
    s = n;
    while(n){ // повторяет пока n не равна 0
        s = n % 2;
        if(s==1)
            c++;
        n/=2;
    }
    cout << c++;
    return 0;
}
