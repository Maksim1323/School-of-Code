№2 - Люблю математику - Дано уравнение вида y = k*x +b. Коэффициенты k, b
действительные и вводятся с консоли (то есть могут быть дробные). Далее 
вводится число 'x' - корень уравнения. Требуется вычислить 'y' по введенному 'x' и 
вывести четверть, в которой лежит точка (x, y). Четверть это область 
координатной плоскости (смотри картинку). 
#include<iostream>
using namespace std;

int main()
{
    float y, k, x, b;
    cin >> k >> b >> x;
    y = k * x + b;
    if (y >= 0 && x >= 0) cout >> I;
    else if (y >= 0 && x < 0) cout >> II;
    else if (y < 0 && x < 0) cout >> III;
    else cout >> IV;
    return 0;
}
