//№0 - Монетки
#include<iostream>
using namespace std;

int main()
{
    int n, vv = 0, nn = 0, N;
    cin >> n;
    for (int i = 0; i <= n - 1; i++){
        cin >> N;
        if (N == 1) vv += 1;
        else nn += 1;
    }
    if (nn <= vv) cout << nn;
    else cout << vv;
    return 0;
}
