#include <iostream>
using namespace std;
int main()
{
    int a, b, c, D;
    cin >> a;
    cin >> b;
    cin >> c;
    D = b * b - 4 * a * c;
    if (D == 0) cout << 1;
    if (D > 0) cout << 2;
    if (D < 0) cout << 0;
}
