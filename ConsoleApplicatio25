#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int N;
    cin >> N;
    vector<int> v(N);
    for (int i = 0; i <= N - 1; i++) cin >> v[i];
    for (int i = N - 1; i >= 1; i--) {
        if (v[i] % 2 == 0) cout << v[i] << " ";
        
    }
    return 0;
}
