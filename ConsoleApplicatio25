#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int N, a;
    cin >> N;
    for (int i = 1; i <= N; i++) cin >> a;
    for (int i = N; i >= 1; i--) {
        if (i % 2 == 0) cout << i << " ";
        
    }
    return 0;
}
