//A. Преобразование числа
#include <iostream>
 
using namespace std;
int main()
{
    int t, x, y;
    cin >> t;
    for (int i = 1; i <= t; i++){
        cin >> x >> y;
        if (y % x != 0) cout << 0 << " " << 0 << endl;
        else cout << 1 << " " << y / x << endl;
    }    
}
   
